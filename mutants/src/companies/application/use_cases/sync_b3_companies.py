import asyncio
import logging
from typing import Dict, Any, List, Optional
from pydantic import ValidationError

from companies.domain.entities.company import Company
from companies.domain.ports.b3_data_source import B3DataSource
from companies.domain.ports.company_repository import CompanyRepository
from shared.infrastructure.progress import ProgressReporter
from shared.infrastructure.utils.date_resilient import DateResilientParser
from shared.infrastructure.monitoring.metrics import metrics
from shared.infrastructure.config import settings
import time

logger = logging.getLogger(__name__)
from typing import Annotated
from typing import Callable
from typing import ClassVar

MutantDict = Annotated[dict[str, Callable], "Mutant"] # type: ignore


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None): # type: ignore
    """Forward call to original or mutated function, depending on the environment"""
    import os # type: ignore
    mutant_under_test = os.environ['MUTANT_UNDER_TEST'] # type: ignore
    if mutant_under_test == 'fail': # type: ignore
        from mutmut.__main__ import MutmutProgrammaticFailException # type: ignore
        raise MutmutProgrammaticFailException('Failed programmatically')       # type: ignore
    elif mutant_under_test == 'stats': # type: ignore
        from mutmut.__main__ import record_trampoline_hit # type: ignore
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__) # type: ignore
        # (for class methods, orig is bound and thus does not need the explicit self argument)
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_' # type: ignore
    if not mutant_under_test.startswith(prefix): # type: ignore
        result = orig(*call_args, **call_kwargs) # type: ignore
        return result # type: ignore
    mutant_name = mutant_under_test.rpartition('.')[-1] # type: ignore
    if self_arg is not None: # type: ignore
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs) # type: ignore
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs) # type: ignore
    return result # type: ignore

class SyncB3CompaniesUseCase:
    """
    Application Use Case to synchronize the list of companies from B3
    into the database using the data source and repository ports.
    """
    def __init__(self, data_source: B3DataSource, repository: CompanyRepository):
        args = [data_source, repository]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁ__init____mutmut_orig'), object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁ__init____mutmut_mutants'), args, kwargs, self)
    def xǁSyncB3CompaniesUseCaseǁ__init____mutmut_orig(self, data_source: B3DataSource, repository: CompanyRepository):
        self._data_source = data_source
        self._repository = repository
        
    def xǁSyncB3CompaniesUseCaseǁ__init____mutmut_1(self, data_source: B3DataSource, repository: CompanyRepository):
        self._data_source = None
        self._repository = repository
        
    def xǁSyncB3CompaniesUseCaseǁ__init____mutmut_2(self, data_source: B3DataSource, repository: CompanyRepository):
        self._data_source = data_source
        self._repository = None
        
    
    xǁSyncB3CompaniesUseCaseǁ__init____mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSyncB3CompaniesUseCaseǁ__init____mutmut_1': xǁSyncB3CompaniesUseCaseǁ__init____mutmut_1, 
        'xǁSyncB3CompaniesUseCaseǁ__init____mutmut_2': xǁSyncB3CompaniesUseCaseǁ__init____mutmut_2
    }
    xǁSyncB3CompaniesUseCaseǁ__init____mutmut_orig.__name__ = 'xǁSyncB3CompaniesUseCaseǁ__init__'
    def _map_b3_payload_to_entity(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        args = [basic_info, detailed_info]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_orig'), object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_mutants'), args, kwargs, self)
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_orig(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_1(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = None

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_2(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            None
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_3(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get(None, "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_4(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", None)
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_5(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_6(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", )
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_7(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("XXindustryClassificationXX", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_8(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryclassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_9(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("INDUSTRYCLASSIFICATION", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_10(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "XXXX")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_11(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = None
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_12(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            None
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_13(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get(None, [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_14(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", None)
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_15(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get([])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_16(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", )
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_17(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("XXotherCodesXX", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_18(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("othercodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_19(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("OTHERCODES", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_20(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=None,
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_21(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=None,
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_22(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=None,
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_23(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=None,
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_24(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=None,
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_25(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=None,
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_26(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=None,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_27(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=None,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_28(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=None,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_29(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=None,
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_30(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=None,
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_31(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=None,
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_32(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=None,
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_33(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=None,
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_34(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=None,
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_35(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=None,
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_36(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=None,
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_37(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=None,
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_38(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=None,
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_39(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=None,
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_40(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=None,
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_41(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=None,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_42(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=None,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_43(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=None,
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_44(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=None,
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_45(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=None,
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_46(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=None
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_47(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_48(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_49(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_50(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_51(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_52(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_53(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_54(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_55(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_56(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_57(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_58(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_59(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_60(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_61(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_62(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_63(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_64(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_65(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_66(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_67(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_68(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_69(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_70(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_71(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_72(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_73(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_74(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") and basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_75(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get(None) or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_76(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("XXissuingCompanyXX") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_77(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingcompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_78(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("ISSUINGCOMPANY") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_79(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get(None),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_80(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("XXissuingCompanyXX"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_81(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingcompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_82(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("ISSUINGCOMPANY"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_83(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(None),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_84(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get(None)),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_85(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("XXcodeCVMXX")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_86(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codecvm")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_87(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("CODECVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_88(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") and basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_89(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get(None) or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_90(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("XXcompanyNameXX") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_91(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyname") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_92(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("COMPANYNAME") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_93(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get(None),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_94(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("XXcompanyNameXX"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_95(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyname"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_96(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("COMPANYNAME"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_97(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get(None),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_98(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("XXtradingNameXX"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_99(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingname"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_100(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("TRADINGNAME"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_101(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get(None),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_102(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("XXcnpjXX"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_103(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("CNPJ"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_104(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get(None),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_105(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("XXmarketXX"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_106(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("MARKET"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_107(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") and detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_108(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get(None) or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_109(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("XXindustryClassificationEngXX") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_110(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryclassificationeng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_111(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("INDUSTRYCLASSIFICATIONENG") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_112(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get(None),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_113(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("XXsegmentEngXX"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_114(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmenteng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_115(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("SEGMENTENG"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_116(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get(None),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_117(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("XXactivityXX"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_118(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("ACTIVITY"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_119(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get(None),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_120(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("XXdescribleCategoryBVMFXX"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_121(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describlecategorybvmf"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_122(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("DESCRIBLECATEGORYBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_123(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(None, "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_124(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), None),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_125(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse("date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_126(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), ),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_127(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") and basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_128(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get(None) or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_129(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("XXdateListingXX") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_130(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("datelisting") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_131(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("DATELISTING") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_132(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get(None), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_133(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("XXdateListingXX"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_134(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("datelisting"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_135(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("DATELISTING"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_136(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "XXdate_listingXX"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_137(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "DATE_LISTING"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_138(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(None, "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_139(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), None),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_140(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse("last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_141(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), ),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_142(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get(None), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_143(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("XXlastDateXX"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_144(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastdate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_145(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("LASTDATE"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_146(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "XXlast_dateXX"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_147(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "LAST_DATE"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_148(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(None, "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_149(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), None),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_150(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse("date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_151(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), ),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_152(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get(None), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_153(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("XXdateQuotationXX"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_154(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("datequotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_155(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("DATEQUOTATION"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_156(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "XXdate_quotationXX"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_157(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "DATE_QUOTATION"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_158(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get(None),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_159(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("XXwebsiteXX"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_160(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("WEBSITE"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_161(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") and detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_162(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get(None) or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_163(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("XXregistrarXX") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_164(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("REGISTRAR") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_165(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get(None),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_166(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("XXinstitutionCommonXX"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_167(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutioncommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_168(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("INSTITUTIONCOMMON"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_169(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") and detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_170(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") and detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_171(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get(None) or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_172(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("XXmainRegistrarXX") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_173(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainregistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_174(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("MAINREGISTRAR") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_175(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get(None) or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_176(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("XXinstitutionPreferredXX") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_177(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionpreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_178(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("INSTITUTIONPREFERRED") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_179(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get(None),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_180(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("XXmain_registrarXX"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_181(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("MAIN_REGISTRAR"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_182(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get(None),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_183(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("XXstatusXX"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_184(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("STATUS"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_185(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get(None),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_186(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("XXtypeXX"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_187(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("TYPE"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_188(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get(None),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_189(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("XXmarketIndicatorXX"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_190(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketindicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_191(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("MARKETINDICATOR"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_192(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get(None),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_193(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("XXtypeBDRXX"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_194(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typebdr"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_195(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("TYPEBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_196(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get(None, detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_197(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", None),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_198(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get(detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_199(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", ),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_200(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("XXhasQuotationXX", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_201(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasquotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_202(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("HASQUOTATION", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_203(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get(None)),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_204(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("XXhas_quotationXX")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_205(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("HAS_QUOTATION")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_206(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get(None, detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_207(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", None),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_208(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get(detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_209(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", ),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_210(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("XXhasEmissionsXX", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_211(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasemissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_212(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("HASEMISSIONS", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_213(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get(None)),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_214(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("XXhas_emissionsXX")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_215(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("HAS_EMISSIONS")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_216(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get(None, detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_217(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", None)
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_218(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get(detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_219(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", )
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_220(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("XXhasBDRXX", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_221(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasbdr", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_222(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("HASBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_223(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get(None))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_224(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("XXhas_bdrXX"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_225(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("HAS_BDR"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingCompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_226(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(None)
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_227(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get(None)}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_228(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('XXissuingCompanyXX')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_229(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('issuingcompany')}: {e}")
            raise e
    def xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_230(self, basic_info: Dict[str, Any], detailed_info: Dict[str, Any]) -> Company:
        """
        Maps raw JSON from B3 into the Company domain entity.
        Includes fallbacks and conversions matching the Domain rules.
        """
        # 1. Process Industry Classification (Sector / Subsector / Segment)
        sector, subsector, segment = self._parse_industry_classification(
            detailed_info.get("industryClassification", "")
        )

        # 2. Extract Security Identifiers (Ticker codes and ISIN codes)
        ticker_codes, isin_codes = self._extract_security_identifiers(
            detailed_info.get("otherCodes", [])
        )
        try:
            return Company(
                ticker=detailed_info.get("issuingCompany") or basic_info.get("issuingCompany"),
                cvm_code=str(basic_info.get("codeCVM")),
                company_name=detailed_info.get("companyName") or basic_info.get("companyName"),
                trading_name=detailed_info.get("tradingName"),
                cnpj=detailed_info.get("cnpj"),
                listing=detailed_info.get("market"),
                sector=sector,
                subsector=subsector,
                segment=segment,
                segment_eng=detailed_info.get("industryClassificationEng") or detailed_info.get("segmentEng"),
                activity=detailed_info.get("activity"),
                describle_category_bvmf=detailed_info.get("describleCategoryBVMF"),
                date_listing=DateResilientParser.parse(detailed_info.get("dateListing") or basic_info.get("dateListing"), "date_listing"),
                last_date=DateResilientParser.parse(detailed_info.get("lastDate"), "last_date"),
                date_quotation=DateResilientParser.parse(detailed_info.get("dateQuotation"), "date_quotation"),
                website=detailed_info.get("website"),
                registrar=detailed_info.get("registrar") or detailed_info.get("institutionCommon"),
                main_registrar=detailed_info.get("mainRegistrar") or detailed_info.get("institutionPreferred") or detailed_info.get("main_registrar"),
                status=detailed_info.get("status"),
                type=detailed_info.get("type"),
                market_indicator=detailed_info.get("marketIndicator"),
                ticker_codes=ticker_codes,
                isin_codes=isin_codes,
                type_bdr=detailed_info.get("typeBDR"),
                has_quotation=detailed_info.get("hasQuotation", detailed_info.get("has_quotation")),
                has_emissions=detailed_info.get("hasEmissions", detailed_info.get("has_emissions")),
                has_bdr=detailed_info.get("hasBDR", detailed_info.get("has_bdr"))
            )
        except ValidationError as e:
            logger.error(f"Validation error for {basic_info.get('ISSUINGCOMPANY')}: {e}")
            raise e
    
    xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_1': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_1, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_2': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_2, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_3': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_3, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_4': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_4, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_5': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_5, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_6': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_6, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_7': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_7, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_8': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_8, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_9': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_9, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_10': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_10, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_11': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_11, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_12': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_12, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_13': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_13, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_14': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_14, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_15': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_15, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_16': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_16, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_17': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_17, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_18': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_18, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_19': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_19, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_20': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_20, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_21': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_21, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_22': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_22, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_23': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_23, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_24': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_24, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_25': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_25, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_26': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_26, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_27': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_27, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_28': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_28, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_29': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_29, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_30': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_30, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_31': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_31, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_32': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_32, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_33': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_33, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_34': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_34, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_35': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_35, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_36': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_36, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_37': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_37, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_38': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_38, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_39': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_39, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_40': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_40, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_41': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_41, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_42': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_42, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_43': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_43, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_44': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_44, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_45': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_45, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_46': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_46, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_47': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_47, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_48': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_48, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_49': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_49, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_50': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_50, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_51': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_51, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_52': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_52, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_53': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_53, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_54': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_54, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_55': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_55, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_56': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_56, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_57': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_57, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_58': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_58, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_59': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_59, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_60': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_60, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_61': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_61, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_62': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_62, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_63': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_63, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_64': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_64, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_65': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_65, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_66': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_66, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_67': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_67, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_68': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_68, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_69': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_69, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_70': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_70, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_71': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_71, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_72': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_72, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_73': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_73, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_74': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_74, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_75': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_75, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_76': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_76, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_77': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_77, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_78': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_78, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_79': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_79, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_80': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_80, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_81': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_81, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_82': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_82, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_83': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_83, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_84': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_84, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_85': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_85, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_86': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_86, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_87': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_87, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_88': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_88, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_89': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_89, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_90': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_90, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_91': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_91, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_92': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_92, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_93': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_93, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_94': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_94, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_95': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_95, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_96': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_96, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_97': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_97, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_98': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_98, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_99': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_99, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_100': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_100, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_101': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_101, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_102': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_102, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_103': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_103, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_104': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_104, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_105': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_105, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_106': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_106, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_107': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_107, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_108': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_108, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_109': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_109, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_110': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_110, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_111': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_111, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_112': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_112, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_113': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_113, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_114': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_114, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_115': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_115, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_116': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_116, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_117': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_117, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_118': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_118, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_119': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_119, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_120': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_120, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_121': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_121, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_122': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_122, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_123': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_123, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_124': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_124, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_125': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_125, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_126': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_126, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_127': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_127, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_128': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_128, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_129': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_129, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_130': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_130, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_131': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_131, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_132': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_132, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_133': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_133, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_134': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_134, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_135': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_135, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_136': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_136, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_137': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_137, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_138': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_138, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_139': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_139, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_140': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_140, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_141': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_141, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_142': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_142, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_143': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_143, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_144': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_144, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_145': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_145, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_146': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_146, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_147': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_147, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_148': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_148, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_149': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_149, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_150': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_150, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_151': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_151, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_152': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_152, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_153': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_153, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_154': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_154, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_155': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_155, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_156': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_156, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_157': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_157, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_158': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_158, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_159': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_159, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_160': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_160, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_161': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_161, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_162': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_162, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_163': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_163, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_164': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_164, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_165': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_165, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_166': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_166, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_167': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_167, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_168': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_168, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_169': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_169, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_170': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_170, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_171': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_171, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_172': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_172, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_173': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_173, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_174': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_174, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_175': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_175, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_176': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_176, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_177': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_177, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_178': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_178, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_179': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_179, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_180': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_180, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_181': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_181, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_182': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_182, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_183': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_183, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_184': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_184, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_185': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_185, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_186': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_186, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_187': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_187, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_188': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_188, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_189': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_189, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_190': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_190, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_191': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_191, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_192': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_192, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_193': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_193, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_194': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_194, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_195': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_195, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_196': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_196, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_197': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_197, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_198': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_198, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_199': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_199, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_200': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_200, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_201': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_201, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_202': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_202, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_203': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_203, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_204': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_204, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_205': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_205, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_206': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_206, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_207': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_207, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_208': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_208, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_209': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_209, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_210': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_210, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_211': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_211, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_212': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_212, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_213': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_213, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_214': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_214, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_215': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_215, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_216': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_216, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_217': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_217, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_218': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_218, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_219': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_219, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_220': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_220, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_221': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_221, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_222': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_222, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_223': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_223, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_224': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_224, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_225': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_225, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_226': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_226, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_227': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_227, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_228': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_228, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_229': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_229, 
        'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_230': xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_230
    }
    xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity__mutmut_orig.__name__ = 'xǁSyncB3CompaniesUseCaseǁ_map_b3_payload_to_entity'

    def _parse_industry_classification(self, industry_raw: str) -> tuple:
        args = [industry_raw]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_orig'), object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_mutants'), args, kwargs, self)

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_orig(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_1(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_2(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = None
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_3(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split(None)]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_4(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("XX/XX")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_5(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_6(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[1] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_7(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) >= 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_8(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 1 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_9(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = None
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_10(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[2] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_11(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) >= 1 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_12(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 2 else sector
        segment = parts[2] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_13(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = None
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_14(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[3] if len(parts) > 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_15(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) >= 2 else sector
        
        return sector, subsector, segment

    def xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_16(self, industry_raw: str) -> tuple:
        """Splits industry classification string into (sector, subsector, segment)."""
        if not industry_raw:
            return None, None, None
            
        parts = [p.strip() for p in industry_raw.split("/")]
        
        sector = parts[0] if len(parts) > 0 else None
        subsector = parts[1] if len(parts) > 1 else sector
        segment = parts[2] if len(parts) > 3 else sector
        
        return sector, subsector, segment
    
    xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_1': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_1, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_2': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_2, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_3': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_3, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_4': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_4, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_5': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_5, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_6': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_6, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_7': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_7, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_8': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_8, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_9': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_9, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_10': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_10, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_11': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_11, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_12': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_12, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_13': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_13, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_14': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_14, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_15': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_15, 
        'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_16': xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_16
    }
    xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification__mutmut_orig.__name__ = 'xǁSyncB3CompaniesUseCaseǁ_parse_industry_classification'

    def _extract_security_identifiers(self, other_codes: List[Dict[str, Any]]) -> tuple:
        args = [other_codes]# type: ignore
        kwargs = {}# type: ignore
        return _mutmut_trampoline(object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_orig'), object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_mutants'), args, kwargs, self)

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_orig(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_1(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_2(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = None
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_3(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["XXcodeXX"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_4(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["CODE"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_5(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) or "code" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_6(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "XXcodeXX" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_7(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "CODE" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_8(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" not in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_9(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = None
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_10(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["XXisinXX"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_11(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["ISIN"] for o in other_codes if isinstance(o, dict) and "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_12(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) or "isin" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_13(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "XXisinXX" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_14(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "ISIN" in o]
        
        return ticker_codes, isin_codes

    def xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_15(self, other_codes: List[Dict[str, Any]]) -> tuple:
        """Extracts ticker and ISIN codes from the otherCodes list."""
        if not other_codes:
            return [], []
            
        ticker_codes = [o["code"] for o in other_codes if isinstance(o, dict) and "code" in o]
        isin_codes = [o["isin"] for o in other_codes if isinstance(o, dict) and "isin" not in o]
        
        return ticker_codes, isin_codes
    
    xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_1': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_1, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_2': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_2, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_3': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_3, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_4': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_4, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_5': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_5, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_6': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_6, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_7': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_7, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_8': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_8, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_9': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_9, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_10': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_10, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_11': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_11, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_12': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_12, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_13': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_13, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_14': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_14, 
        'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_15': xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_15
    }
    xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers__mutmut_orig.__name__ = 'xǁSyncB3CompaniesUseCaseǁ_extract_security_identifiers'

    async def execute(self) -> None:
        args = []# type: ignore
        kwargs = {}# type: ignore
        return await _mutmut_trampoline(object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_orig'), object.__getattribute__(self, 'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_mutants'), args, kwargs, self)

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_orig(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_1(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info(None)
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_2(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("XXStarting B3 Companies SynchronizationXX")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_3(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("starting b3 companies synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_4(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("STARTING B3 COMPANIES SYNCHRONIZATION")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_5(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = None
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_6(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = None
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_7(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = None
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_8(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(None)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_9(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = None
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_10(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=None)
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_11(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = None
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_12(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get(None)
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_13(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("XXissuingCompanyXX")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_14(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingcompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_15(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("ISSUINGCOMPANY")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_16(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = None
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_17(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(None)
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_18(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get(None))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_19(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("XXcodeCVMXX"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_20(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codecvm"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_21(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("CODECVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_22(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker and not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_23(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_24(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_25(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = None

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_26(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(None)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_27(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = None
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_28(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(None, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_29(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, None)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_30(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_31(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, )
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_32(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(None)
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_33(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(None, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_34(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, None))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_35(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress([ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_36(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, ))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_37(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = None
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_38(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(None, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_39(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, None) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_40(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_41(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, ) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_42(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(None)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_43(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = None
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_44(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = None
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_45(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_46(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = None
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_47(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list(None)
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_48(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(None)
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_49(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(None)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_50(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(None)
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_51(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context=None, source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_52(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source=None).inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_53(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_54(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", ).inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_55(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="XXcompaniesXX", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_56(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="COMPANIES", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_57(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="XXb3XX").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_58(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="B3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_59(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = None
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_60(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = None
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_61(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = None
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_62(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) - 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_63(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(None, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_64(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, None) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_65(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_66(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, ) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_67(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 1) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_68(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 2
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_69(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = None
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_70(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) - 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_71(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(None, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_72(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, None) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_73(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_74(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, ) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_75(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 1) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_76(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 2
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_77(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(None)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_78(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=None).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_79(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(None)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_80(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=None).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_81(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = None
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_82(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() + start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_83(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(None)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_84(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context=None).observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_85(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="XXcompaniesXX").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_86(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="COMPANIES").observe(duration)
            logger.info(f"Synchronization completed successfully in {duration:.2f}s.")
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()

    async def xǁSyncB3CompaniesUseCaseǁexecute__mutmut_87(self) -> None:
        """
        Main execution flow with SOTA concurrency:
        1. Fetch all companies listed.
        2. Fetch details for each company concurrently with rate limiting.
        3. Map to Domain Entities.
        4. Save to Repository in high-performance batches.
        """
        logger.info("Starting B3 Companies Synchronization")
        start_time = time.perf_counter()
        
        # 1. SATURATION: Mark task as active
        metrics.active_sync_tasks.inc()
        
        try:
            async with self._data_source:
                # 1. Fetch the raw initial list
                initial_companies = await self._data_source.fetch_initial_companies()
                
                # Using concurrency from centralized configuration
                semaphore = asyncio.Semaphore(settings.b3.max_concurrency)
                reporter = ProgressReporter(total=len(initial_companies))
                
                async def process_company(index: int, raw_company: Dict[str, Any]) -> Optional[Company]:
                    ticker = raw_company.get("issuingCompany")
                    cvm_code = str(raw_company.get("codeCVM"))
                    
                    if not ticker or not cvm_code.isdigit():
                        return None
                        
                    async with semaphore:
                        try:
                            # 2. Detail fetch
                            details = await self._data_source.fetch_company_details(cvm_code)

                            # 3. Domain Mapping 
                            entity = self._map_b3_payload_to_entity(raw_company, details)
                            
                            logger.info(reporter.get_formatted_progress(index, [ticker]))
                            return entity
                        except Exception:
                            # Silently continue to process others, reported by error metrics if needed
                            return None

                # Execute all tasks concurrently
                tasks = [process_company(i, raw) for i, raw in enumerate(initial_companies)]
                results = await asyncio.gather(*tasks)
                
                # Filter out None results from skips or errors
                entities_to_save = [e for e in results if e is not None]
                        
                # 4. Persistence
                if entities_to_save:
                    # Deduplicate by ticker to prevent conflicts within the same batch
                    unique_entities = list({e.ticker: e for e in entities_to_save}.values())
                    logger.info(f"Saving {len(unique_entities)} unique companies to the repository.")
                    self._repository.save_batch(unique_entities)
                    
                    # 2. BUSINESS OUTCOMES: Telemetry
                    metrics.entities_synced_total.labels(context="companies", source="b3").inc(len(unique_entities))
                    
                    # 3. MARKET INSIGHTS: Update snapshot gauges
                    # Note: In a real system, these would be aggregated from the DB periodically
                    # but updating here provides real-time visibility post-sync.
                    sectors = {}
                    segments = {}
                    for e in unique_entities:
                        if e.sector:
                            sectors[e.sector] = sectors.get(e.sector, 0) + 1
                        if e.listing:
                            segments[e.listing] = segments.get(e.listing, 0) + 1
                    
                    for sector, count in sectors.items():
                        metrics.companies_by_sector.labels(sector=sector).set(count)
                    for segment, count in segments.items():
                        metrics.companies_by_segment.labels(segment=segment).set(count)
            
            duration = time.perf_counter() - start_time
            metrics.sync_duration_seconds.labels(context="companies").observe(duration)
            logger.info(None)
            
        finally:
            # Always decrement saturation gauge
            metrics.active_sync_tasks.dec()
    
    xǁSyncB3CompaniesUseCaseǁexecute__mutmut_mutants : ClassVar[MutantDict] = { # type: ignore
    'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_1': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_1, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_2': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_2, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_3': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_3, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_4': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_4, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_5': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_5, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_6': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_6, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_7': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_7, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_8': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_8, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_9': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_9, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_10': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_10, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_11': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_11, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_12': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_12, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_13': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_13, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_14': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_14, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_15': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_15, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_16': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_16, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_17': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_17, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_18': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_18, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_19': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_19, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_20': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_20, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_21': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_21, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_22': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_22, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_23': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_23, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_24': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_24, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_25': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_25, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_26': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_26, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_27': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_27, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_28': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_28, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_29': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_29, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_30': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_30, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_31': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_31, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_32': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_32, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_33': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_33, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_34': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_34, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_35': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_35, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_36': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_36, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_37': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_37, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_38': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_38, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_39': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_39, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_40': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_40, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_41': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_41, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_42': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_42, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_43': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_43, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_44': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_44, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_45': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_45, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_46': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_46, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_47': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_47, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_48': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_48, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_49': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_49, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_50': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_50, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_51': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_51, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_52': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_52, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_53': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_53, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_54': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_54, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_55': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_55, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_56': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_56, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_57': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_57, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_58': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_58, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_59': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_59, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_60': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_60, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_61': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_61, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_62': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_62, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_63': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_63, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_64': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_64, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_65': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_65, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_66': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_66, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_67': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_67, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_68': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_68, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_69': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_69, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_70': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_70, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_71': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_71, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_72': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_72, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_73': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_73, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_74': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_74, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_75': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_75, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_76': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_76, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_77': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_77, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_78': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_78, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_79': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_79, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_80': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_80, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_81': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_81, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_82': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_82, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_83': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_83, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_84': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_84, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_85': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_85, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_86': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_86, 
        'xǁSyncB3CompaniesUseCaseǁexecute__mutmut_87': xǁSyncB3CompaniesUseCaseǁexecute__mutmut_87
    }
    xǁSyncB3CompaniesUseCaseǁexecute__mutmut_orig.__name__ = 'xǁSyncB3CompaniesUseCaseǁexecute'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/flyai_b3
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - flyai-net

  # Example Worker manifesting the same image
  worker-companies:
    build: .
    command: ["python", "-m", "src.shared.presentation.cli", "sync-companies"] # Placeholder for now
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/flyai_b3
    depends_on:
      - db
    networks:
      - flyai-net

  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=flyai_b3
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - flyai-net
    volumes:
      - /media/stangler/Linux_D/FLY.AI_data/postgres:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - flyai-net
    volumes:
      - /media/stangler/Linux_D/FLY.AI_data/redis:/data

  pgadmin:
    image: dpage/pgadmin4
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@fly.ai
      - PGADMIN_DEFAULT_PASSWORD=admin
    ports:
      - "5050:80"
    networks:
      - flyai-net
    depends_on:
      - db

networks:
  flyai-net:
    driver: bridge
